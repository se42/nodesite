h3 Candle Lighting Visualization (N = {{size}})
h4 Number of candles:  {{numberOfCandles}}
h4 Seconds passed:  {{currentStep}}

| {{#each grid as row}}
| {{#each row}}
| {{#if isLit}}
i(class="{{repr}}" aria-hidden="true" lit)
| {{else}}
i(class="{{repr}}" aria-hidden="true")
| {{/if}}
| {{/each}}
br
| {{/each}}